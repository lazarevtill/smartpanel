# Smart Panel v2.0 Requirements
# Matter-Enabled Raspberry Pi Control Panel

# Core functionality
gpiozero>=1.6.0
luma.lcd>=2.9.0
Pillow>=10.0.0
psutil>=5.8.0
RPi.GPIO>=0.7.0

# Note: lgpio and spidev are installed as system packages via apt
# They will be available via --system-site-packages flag in venv

# QR Code generation for Matter commissioning
qrcode[pil]>=7.0

# Matter IoT support - REAL Matter protocol
python-matter-server>=8.0.0
zeroconf>=0.148.0
aiohttp>=3.13.0

# Development and testing (optional)
# pytest>=7.0.0  # Uncomment for testing
# pytest-cov>=4.0.0  # Uncomment for coverage reports
# flake8>=6.0.0  # Uncomment for linting
# black>=23.0.0  # Uncomment for code formatting
